package org.example.financetrackerapi.user.entity;

public class TestUser extends User {
   public TestUser(Long id, String email, String hashPassword) {
        super(id, email, hashPassword);
    }
}
